import 'package:flutter/cupertino.dart';

class ComingMovie extends StatelessWidget {
  const ComingMovie({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
