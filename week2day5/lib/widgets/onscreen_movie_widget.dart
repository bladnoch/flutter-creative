import 'package:flutter/cupertino.dart';

class OnScreenMovie extends StatelessWidget {
  const OnScreenMovie({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
